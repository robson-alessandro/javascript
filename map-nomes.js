// tornar todas as letras padrao (letras maiúsculas )


const nomes = ["ana Julia", "Caio vinicius", "BIA silva"];

const nomesPadronizados = nomes.map((nomes) => nomes.toUpperCase())

console.log(nomesPadronizados)